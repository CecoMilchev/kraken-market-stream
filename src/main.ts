import fs from 'fs';
import { hostname } from 'os';
import axios from 'axios';

console.log('Kraken Market Stream - Main Application');
console.log('Services are now distributed across microservices architecture');
console.log('Please use the individual service runners or start all services with:');
console.log('npm run start:all');

// This main.ts file is now just a placeholder
// The actual orderbook functionality has been moved to the orderbook-service
// Use the following commands to run services:
// - npm run start:api-gateway
// - npm run start:auth-service  
// - npm run start:user-service
// - npm run start:orderbook-service

process.exit(0);
